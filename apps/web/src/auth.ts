// Auth utility - all auth now handled client-side via localStorage JWT tokens.
// Kept as placeholder. Import getToken/setToken/clearAuth from '@/lib/auth' instead.
export const auth = () => new Response(null, { status: 401 });
