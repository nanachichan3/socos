version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: docker/Dockerfile.web
      args:
        NEXT_PUBLIC_BACKEND_URL: ${NEXT_PUBLIC_BACKEND_URL:-https://socos.rachkovan.com}
        NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL:-https://socos.rachkovan.com}
    environment:
      - NODE_ENV=production
    ports:
      - "3000:3000"
    networks:
      - coolify
      - swwcg80gkw4k0k4oco8w8wgw
    restart: unless-stopped

  api:
    build:
      context: .
      dockerfile: services/api/Dockerfile
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://postgres:37BLEWztnVO7AqI8bQb9vUrCnnBif8uaThihxv4K9R7Nsa7AiRiywB4K1Ob2nZIi@zwkk0scogckskkwss8oo48k4:5432/socos?sslmode=disable
      - NODE_TLS_REJECT_UNAUTHORIZED=0
      - JWT_SECRET=${JWT_SECRET:-socos-dev-secret-2026}
      - PORT=3001
    expose:
      - "3001"
    networks:
      - coolify
      - swwcg80gkw4k0k4oco8w8wgw
    restart: unless-stopped

networks:
  coolify:
    external: true
  swwcg80gkw4k0k4oco8w8wgw:
    external: true
