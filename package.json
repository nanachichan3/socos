{
  "name": "socos",
  "private": true,
  "workspaces": [
    "services/api",
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build"
  },
  "engines": {
    "node": ">=22"
  }
}
